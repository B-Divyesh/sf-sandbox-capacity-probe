{
  "name": "sandbox-capacity-probe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "test:unit": "vitest run --config site/vitest.config.ts && cargo test --manifest-path cli/Cargo.toml",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run build:site && npm run test:e2e",
    "build:cli": "cargo build --release --manifest-path cli/Cargo.toml",
    "build:site": "vite build --config site/vite.config.ts",
    "build": "npm run test:unit && npm run build:cli && npm run build:site"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "typescript": "5.9.2",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
